<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- the speech API host keeps serving while the app is in the background -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- screen-off generation: Samsung throttles background CPU without it -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <uses-feature android:name="android.hardware.vulkan.compute" android:required="false" />
    <uses-feature android:name="android.hardware.vulkan.version" android:version="0x00400003" android:required="false" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:theme="@style/Theme.TTSRunner">

        <!-- vendor OpenCL drivers, resolved through the bundled dlopen shim -->
        <uses-native-library android:name="libOpenCL.so" android:required="false" />
        <uses-native-library android:name="libOpenCL_adreno.so" android:required="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- record a reference voice by reading the standard passage -->
        <activity
            android:name=".RecordActivity"
            android:exported="false" />

        <!-- trim a picked recording to a section before it becomes a speaker -->
        <activity
            android:name=".TrimActivity"
            android:exported="false" />

        <!-- live conversation mode: type a line, hear it immediately -->
        <activity
            android:name=".TalkActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- transport for a page being read aloud right now -->
        <activity
            android:name=".PlayerActivity"
            android:exported="false" />

        <!-- share-sheet / text-selection target: floats as a dialog -->
        <activity
            android:name=".ShareActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.TTSRunner.Dialog">
            <intent-filter android:label="@string/share_label">
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter android:label="@string/share_label">
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- HTTP host for the OpenAI-compatible speech API and its web client -->
        <service
            android:name=".HostingService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- share-as-m4a for locally generated audio -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.techhurts.ttsrunner.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/share_paths" />
        </provider>

        <!-- separate process: a native crash in llama.cpp only kills :engine.
             exported=true so `adb shell am start-foreground-service` can drive
             it for automated testing (no permission-protected data exposed;
             worst case someone makes the phone talk). -->
        <service
            android:name=".TtsService"
            android:process=":engine"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback" />

        <!-- dead-man alarm target: revives a save job whose engine process the
             OS killed (sticky restart only works once on Samsung) -->
        <receiver
            android:name=".ResumeReceiver"
            android:process=":engine"
            android:exported="false" />

        <!-- background "Refine this voice": a long CPU search that must survive
             the screen going off, so it runs foreground with a silent-audio
             keepalive (media playback) like the save jobs.
             exported=true for the same reason as TtsService: so
             `adb shell am start-foreground-service` can drive it for automated
             testing. It only reads paths as itself and writes styles into the
             app's own voice store; worst case someone burns battery. -->
        <service
            android:name=".RefineService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback" />
    </application>
</manifest>
